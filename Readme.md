# 1. APPLIED DATA SCIENCE WITH PYTHON
### Installation of WSL on windows
> wsl --install

### Linux Commands
> sudo apt update && sudo apt upgrade -y

> sudo apt install python3 python3-pip python3-venv -y

### Create Virtual Environment of Python Project
> python3 -m venv "directoryName"

> source "directoryName"/bin/activate 

### Install Python Libraries
> pip install numpy pandas matpoltlib scikit-learn jupyter