# Ensemble Learning
> Combine multiple models to create strong predicatin system.

- Instead of one genius model, ML often prefer many average model together

## Random Forest Algrithm
> Many decision tree together and training each differently.

- Parallel independent trees and learnings

1. Each tree gets random subset of data & random subset of feature
2. Train many trees
3. combine predications- majority voting

## Boosting
> Each model tries to fix previous model error (sequentially learning)

### 1. Adaboost (Adapative Boosting)
- Reweight samples

### 2. Gradient Boosting
- learns residual error